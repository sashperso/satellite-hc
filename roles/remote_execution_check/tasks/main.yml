---
# tasks file for remote_execution_check

- name: Show any jobs scheduled for remote execution
  ansible.builtin.command: hammer job-template list
  register: result_remote_execution
  ignore_errors: true